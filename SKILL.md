---
name: multi-session-project
description: 大型项目多会话开发法——用PRD统一跨会话认知、用接口契约做部件组装标准、把任务拆成会话级任务包分别实施最后组装，解决单会话上下文过长导致的质量下降；含状态门、跨文档一致性分析、交接简报、需求追踪四条强化机制
type: prompt
whenToUse: 当项目大到单个会话装不下（功能多、周期长、需跨多个会话开发）时使用；用户提到"多会话""拆任务""上下文太长""分部件开发"时必用
---

# 大型项目多会话开发法

把项目当汽车造：先定整车图纸（PRD），再定零部件接口标准（接口契约），
每个会话只造一个部件（任务包），最后按契约总装（集成会话）。
核心目的：对抗单会话上下文膨胀导致的质量下降与信息丢失。

> v1.1 起吸收 GitHub spec-kit / pi-sdd-kit 的长处：状态门、跨文档一致性分析、
> 交接简报、需求追踪（出处见文末"借鉴来源"）。

## 适用范围

- 功能超过 3 个模块、预计开发跨越多个会话的项目。
- 小项目、一次性脚本不要用——这套流程本身有成本。

## 五步工作流

### 第 1 步：目标确认（立项会话内完成）
与用户确认目标卡：真实目标、完成标准、范围边界、关键约束。用户确认后才进入下一步。
含澄清前置：把含糊处当场问清（给 2~4 个选项+影响），不把歧义带进 PRD。

### 第 2 步：PRD（产品需求文档）——跨会话的"宪法"
- 内容：产品定位、用户与场景、完整工作流、功能需求（编号 F1/F2…逐条）、
  非功能需求、明确不做的范围、验收标准、变更记录表。
- 需求写法：每条功能需求写成**可判定的句子**（"当…时，系统应当…"），
  让执行会话不用猜（借鉴 EARS 思想，句式不强制）。
- 权威等级：任何会话对需求理解有冲突，以 PRD 为准；改 PRD 必须经用户确认并登记变更记录。
- 位置：项目根目录 `docs/产品需求文档-PRD.md`。

### 第 3 步：接口契约——部件组装标准
- 内容：模块边界与数据流图、目录布局、全部数据 schema（含示例 JSON）、
  模块间 API 签名、组装与验收约定、**契约冻结规则**（任何会话要改契约，先停下报告，
  用户确认后改文档升版本号再继续，禁止私下偏离）。
- 关键设计原则：
  - 模块间只通过契约文件/契约 API 通信，禁止直读对方内部实现；
  - schema 有代码级唯一权威来源（如 data_models.py），并配 JSON 校验器当"检具"；
  - 所有跨表引用用统一 id 前缀规范。
- 位置：`docs/接口契约.md`。

### 第 4 步：会话任务拆解
- 把项目拆成任务包 S0/S1/S2…，每个任务包**一个会话能装下**为粒度标准，写明：
  产出、验收标准、依赖关系、以及一段可直接粘贴的"会话开场白"（含必读文档路径+任务边界）。
- 排依赖顺序，标出可并行的包；最后一个包固定是"集成组装与总验收"。
- 位置：`docs/会话任务拆解.md`（即总任务规划书，A+B+C…=总计划）。

### 第 4.5 步：任务包计划书（细分计划书）
- 每个任务包一份计划书（`docs/任务包计划书/Sx-名称.md`），固定章节：
  目标 / 范围（做与不做）/ **上游接口**（从哪些任务包拿什么，引用契约条目）/
  **下游接口**（要向哪些任务包交什么，引用契约条目）/ 实施步骤 / 验收标准 / 风险。
- 上下游接口两节是灵魂：接口细节一律引用《接口契约.md》的 schema/API 条目，
  不在计划书里另起炉灶定义接口——接口唯一定义在契约，计划书只做引用和衔接说明。
- 近端任务包（马上要做的）写满细节；远端任务包允许先写框架，轮到它开工的会话里再细化。

### 第 4.8 步：跨文档一致性分析（强制，借鉴 spec-kit /analyze）
- **施工开始前**，对照检查 PRD ↔ 契约 ↔ 任务拆解 ↔ 各计划书：需求有没有没归口的、
  接口有没有缺口、归属有没有冲突。发现的缝隙按冻结规则补登契约并升版本号。
- 这是实证高价值步骤（首个实例一次抓出 10 处缝隙），不许省。

### 第 5 步：分会话实施与组装
- **执行载体二选一，开工前让用户选一次（默认子代理）**：
  - **子代理模式（默认）**：总控会话把任务包简报按"新会话零上下文"标准写全
    （必读文档精确路径、项目现状关键事实、任务边界、纪律、交付格式），派子代理实施；
    子代理就是"新会话"。总控会话只做：文档升版、派工简报、交付证据复核（亲自复跑测试/
    抽查产物）、进度总览更新、向用户汇报。
  - **真·新会话模式**：用户自己开新会话、粘贴任务包开场白开工（开场白模板在任务拆解里）；
    适合用户想分时段、分工具推进，或单个子代理上下文吃紧的超大包。
  - 选择时机：第 4 步任务拆解完成后、第一个实施包开工前问一次即可；中途可切换，
    切换不改变任何文档与契约（两种模式共用同一套 PRD/契约/计划书/交付报告）。
- 每个实施会话（无论哪种载体）：先读 PRD+契约+进度总览 → 只做自己包内的事 →
  交付模块代码 + 契约级测试（喂样例输入、断言输出符合 schema）。
- 集成会话（总装车间）：按契约数据流联调，schema 校验逐环过，任何一环不过即定位为该模块问题，
  只做联调与缺陷修复，不新增功能。
- 按 PRD 验收标准逐条总验收（即 spec-kit 的 converge 思想：对照规格评估产物，差距追加为缺陷项修掉）。

## 强化机制一：状态门（借鉴 pi-sdd-kit .status）

- 每个任务包有且仅有以下状态：**待开工 / 进行中 / 待验收 / 已通过 / 受阻**。
- **草稿不等于通过**：AI 声称完成不算数，状态推进只认证据（契约级测试通过、
  schema 校验通过、验收标准逐条有命令+输出佐证）。
- `docs/进度总览.md` 是状态面板与跨会话接力点：每个包的状态、关键产出、验证基线、
  待用户确认事项全部登记在案，任何新会话先读它。

## 强化机制二：交接简报（借鉴 pi-sdd-kit handoff）

每个任务包会话结束时必须产出交付报告（`docs/交付报告/Sx-名称-交付报告.md`），固定含：
1. 产出文件清单；2. 验收证据（每条验收标准：命令+输出摘要，不许虚报）；
3. 契约偏差/补登记录（无则写无）；4. 遗留问题与标疑；5. **给下游任务包的就绪度说明**
（下游拿什么、注意什么、有哪些在案缺口）。下一个会话靠它接力，不靠记忆。

## 强化机制三：需求追踪（借鉴 pi-sdd-kit traceability）

- PRD 功能需求编号（F1/F2…）→ 归属任务包 → 验收证据，三者要有映射；
- 在任务拆解或进度总览里维护这张映射表；总装验收时按它查漏——
  没有任务包认领的需求是漏项，没有需求来源的功能是蔓延。

## 纪律清单（每个任务会话开工先自查）

1. 已读 PRD、接口契约、进度总览？没有就先读，别开工。
2. 只做自己任务包范围内的事，发现别的问题记录并报告，不顺手改。
3. 要改 PRD 或契约？停下，报告用户，确认后改文档升版本号再继续。
4. 交付必须带契约级测试并跑通；状态推进只认证据。
5. 会话结束时按"交接简报"格式写交付报告，更新进度总览状态。

## 参考实例

本法已在一个真实中型项目（市政管网算量与清单桌面软件，十余个任务包）完整走通：
PRD、接口契约（含数据 schema 与 API 签名）、会话任务拆解（含开场白模板）、
任务包计划书、交付报告、进度总览齐全；并验证过"中途插入新需求"的处理路径——
PRD/契约/任务拆解先升版、补任务包再施工。新项目实施时可参照该文档结构与粒度。

## 语言策略

技能本体与文档结构保持通用；项目产物（PRD、计划书、报告等）用用户的语言书写；
文件名、状态值、id 前缀等技术标识保持稳定不翻译。

## 常见坑

- PRD 写成流水账没有"不做的范围"——范围边界必须明确，否则会话各自发挥必跑偏。
- 契约只有口头约定没有 schema 示例和校验器——组装时才发现各说各话。
- 任务包切太大，单会话又爆了上下文——宁小勿大，半个会话能完成最好。
- 跳过集成会话直接宣布完工——契约级联调和 PRD 逐条验收不可省。
- 跳过跨文档一致性分析直接施工——缝隙会在组装时以十倍代价爆出来。
- 把"AI 说完成了"当完成——没有证据的状态推进等于没有推进。

## 借鉴来源

- GitHub spec-kit（github/spec-kit）：跨文档一致性分析（analyze）、收敛评估（converge）、澄清前置（clarify）。
- pi-sdd-kit（felipefontoura/pi-sdd-kit）：状态门（.status，草稿≠批准）、交接简报（handoff brief）、
  可追踪 ID、EARS 需求句式、steering 持久上下文、语言策略。
