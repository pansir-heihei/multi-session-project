---
name: multi-session-project
description: 项目拆分组装法（原大型项目多会话开发法）——用总框架（PRD+接口契约）约束全局、拆分细分模块、每模块细分框架预留上下游契约接口、最后组装；以省 token 为一级目标（20 万 tokens 触发线是成本机制，与模型上下文长短无关），对抗长上下文质量下降与信息丢失；含状态门、跨文档一致性分析、交接简报、需求追踪、文档漂移检查
type: prompt
whenToUse: 预计项目上下文消耗超 20 万 tokens（成本机制）、功能模块超 3 个且跨多会话、或预计派生子代理 ≥4 个时使用；用户提到"项目拆分组装法""多会话""拆任务""分部件开发"时必用
---

# 项目拆分组装法（原「大型项目多会话开发法」）SKILL v1.2

> 人用名称：**项目拆分组装法**（用户 2026-08-14 命名）；技术标识 `multi-session-project` 保持不变（技能目录、GitHub 仓库、触发引用不动，避免断链）。
> 用户一句话本质：**用总框架约束整个项目 → 拆分细分模块 → 每个独立模块有自己的细分框架、预留上下游契约接口 → 最后各模块组装。**

## 核心目的（按优先级）

1. **节省 token（用户核心目的）**：额度有限，用最少 token 做最多事。总框架一次写清，模块会话不再重复解释；每个会话上下文短，重读成本低。
2. **质量**：对抗长上下文膨胀导致的注意力稀释——会话越长，越容易忘掉自己定的约定、悄悄跑偏。
3. **信息**：文档代替记忆，跨会话、跨工具不丢信息。

## 适用范围与触发判据（满足任一即启用）

- 预计单会话累计上下文消耗超过 **20 万 tokens**（约 1M 上限的 20%）。
  **这是成本机制，不是容量机制**：模型上下文再长也要拆——上下文重读成本随轮次累积，长上下文同样稀释注意力。
- 功能模块超过 3 个、预计跨多会话。
- （v1.2 新增）主会话预计派生子代理 ≥ 4 个：编排复杂度是「量级」之外的第二维度。
- 小项目、一次性脚本不启用——这套流程本身有成本。

## 全流程总览（八步）

用户四步骨架（总框架 → 拆分 → 细分框架+契约接口 → 组装）方向正确，但作为完整方案缺四步与三条暗线。优化后的全流程：

```
① 总框架（PRD 宪法 + 接口契约 + 验收标准，含「不做的范围」）
② 拆分（依赖排序+并行分组；粒度平衡；易变隔离）
③ 细分框架 + 上下游契约接口（稳定边界先冻结，细节近端细化）
④ 一致性分析 + 最小验证（施工前对缝查缺口；花最小成本试通最险/最没把握的一段，不成立回退改总框架）
⑤ 分阶段实施（每阶段对照总框架校验「还在对的路上」，偏了立即停）
⑥ 组装（按数据流逐环联调；组装 ≠ 完工）
⑦ 总验收（对照总框架验收标准逐条过，差距返修）
⑧ 复盘入库（踩坑、决策写回知识库，反哺下一个项目的①）
```

三条贯穿全程的暗线：
- **状态门 + 进度总览**：全程可见，任何会话/工具先读它再干活。
- **变更通道**：需求变 → 用户确认 → PRD/契约升版 → 补任务包（清标场景实例）。
- **文档即事实源**：docs/ 是唯一真理源，工具私有记忆只是缓存。

缺④⑤⑦⑧会导致：框架没对缝/最险段没试（错了拆完才发现）、偏了不停（越干越远）、组装完不知算不算成（没验收标准）、教训不留（下个项目重踩坑）。

## 术语对照（用户词汇 ↔ 技能文档）

| 用户说法 | 技能文档 |
|---|---|
| 总框架 | `docs/产品需求文档-PRD.md`（宪法）+ `docs/接口契约.md`（组装标准） |
| 拆分细分模块 | 任务包 S0 / S1 / S2… |
| 细分框架 | `docs/任务包计划书/Sx-名称.md` |
| 上下游契约接口 | 计划书「上游接口 / 下游接口」节 + 契约冻结规则 |
| 组装 | 集成会话（总装车间） |

## 七步工作流

### 第 1 步：目标确认（立项会话内完成）
与用户确认目标卡：真实目标、完成标准、范围边界、关键约束，用户确认后才进入下一步。
含澄清前置：把含糊处当场问清（给 2~4 个选项 + 影响），不把歧义带进 PRD。

### 第 2 步：总框架之 PRD——跨会话「宪法」
- 内容：产品定位、用户与场景、完整工作流、功能需求（编号 F1/F2… 逐条）、非功能需求、明确不做的范围、验收标准、变更记录表。
- 需求写法：每条功能需求写成**可判定的句子**（「当…时，系统应当…」），让执行会话不用猜。
- 权威等级：任何会话对需求理解有冲突，以 PRD 为准；改 PRD 必须经用户确认并登记变更记录。
- 位置：项目根目录 `docs/产品需求文档-PRD.md`。

### 第 3 步：总框架之接口契约——组装标准
- 内容：模块边界与数据流图、目录布局、全部数据 schema（含示例 JSON）、模块间 API 签名、组装与验收约定、**契约冻结规则**（任何会话要改契约，先停下报告，用户确认后改文档升版本号再继续，禁止私下偏离）。
- 关键设计原则：
  - 模块间只通过契约文件 / 契约 API 通信，禁止直读对方内部实现；
  - schema 有代码级唯一权威来源（如 data_models.py），并配 JSON 校验器当「检具」；
  - 所有跨表引用用统一 id 前缀规范。
- 位置：`docs/接口契约.md`。

### 第 4 步：拆分——会话任务拆解
- 把项目拆成任务包 S0/S1/S2…，以**一个会话能装下**为粒度标准（宁小勿大，半个会话能完成最好），写明：产出、验收标准、依赖关系、以及一段可直接粘贴的「会话开场白」（含必读文档路径 + 任务边界）。
- 排依赖顺序，标出可并行的包；最后一个包固定是「集成组装与总验收」。
- **粒度平衡**：拆细了接口变多、组装成本上升（像分标段——标段越多，界面管理费越高）；拆粗了单会话装不下。最优粒度 = 「会话装得下」与「接口尽量少」的平衡点。
- **易变隔离**：把预计会变的需求圈进少数模块，返工时改动面最小。
- 位置：`docs/会话任务拆解.md`（总任务规划书）。

### 第 5 步：细分框架——任务包计划书
- 每个任务包一份计划书（`docs/任务包计划书/Sx-名称.md`），固定章节：目标 / 范围（做与不做）/ **上游接口**（从哪些任务包拿什么，引用契约条目）/ **下游接口**（要向哪些任务包交什么，引用契约条目）/ 实施步骤 / 验收标准 / 风险。
- 上下游接口两节是灵魂：接口细节一律引用《接口契约.md》的 schema / API 条目，不在计划书里另起炉灶定义接口——接口唯一定义在契约，计划书只做引用和衔接说明。
- 近端任务包写满细节；远端任务包先写框架，轮到它开工的会话里再细化。

### 第 6 步：跨文档一致性分析（强制）
- **施工开始前**，对照检查 PRD ↔ 契约 ↔ 任务拆解 ↔ 各计划书：需求有没有没归口的、接口有没有缺口、归属有没有冲突。发现的缝隙按冻结规则补登契约并升版本号。
- 实证高价值步骤（首个实例一次抓出 10 处缝隙），不许省。

### 第 7 步：实施与组装
- **开工顺序（不跳步）**：路径规划（拆 2~5 阶段）→ 最小验证（先花最小成本试通最险/最没把握的一段；不成立回退改总框架，不带着风险全面铺开）→ 分阶段实施（每阶段结束对照总框架校验「还在对的路上」，偏了立即停）。
- **执行载体四选一，开工前让用户选一次（默认后台子代理）**：
  1. **后台子代理模式（默认）**：总控会话把任务包简报按「新会话零上下文」标准写全（必读文档精确路径、项目现状关键事实、任务边界、纪律、交付格式），派子代理实施；子代理就是「新会话」。总控只做：文档升版、派工简报、交付证据复核（亲自复跑测试 / 抽查产物）、进度总览更新、向用户汇报。
  2. **DSH 目标自动续轮模式（v1.2 新增）**：长线任务包挂 goal 跨轮推进。注意：每轮重读上下文有成本，包仍要小；进度总览仍是权威，goal 只是引擎。
  3. **Ralph 验证轮（v1.2 新增，可选）**：验收前派一个「零历史新大脑」独立复核交付物，防「自己人验收自己人」。
  4. **真·新会话模式**：用户自己开新会话、粘贴任务包开场白开工；适合分时段、分工具推进。
  - 选择时机：第 4 步拆解完成后、第一个实施包开工前问一次即可；中途可切换，切换不改变任何文档与契约。
- 每个实施会话（无论哪种载体）：先读 PRD + 契约 + 进度总览 → 只做自己包内的事 → 交付模块代码 + 契约级测试（喂样例输入、断言输出符合 schema）。
- 集成会话（总装车间）：按契约数据流联调，schema 校验逐环过，任何一环不过即定位为该模块问题，只做联调与缺陷修复，不新增功能。
- 按 PRD 验收标准逐条总验收：对照规格评估产物，差距追加为缺陷项修掉。
- **复盘与记忆**：总结可复用逻辑，把目标确认卡、关键决策、踩坑写入知识库——反哺下一个项目的总框架。

## 新增原则：文档即跨工具事实源（v1.2）

- `docs/` 是项目状态的**唯一真理源**。任何工具的私有记忆（DSH goal、Kimi 知识图谱等）只是缓存；冲突时一律以 `docs/` 为准。
- 这保证 Kimi / DSH / Cursor / 未来任何工具随时接力，与用户画像区的跨模型原则一致。

## 强化机制一：状态门

- 每个任务包有且仅有以下状态：**待开工 / 进行中 / 待验收 / 已通过 / 受阻**。
- **草稿不等于通过**：AI 声称完成不算数，状态推进只认证据（契约级测试通过、schema 校验通过、验收标准逐条有命令 + 输出佐证）。
- `docs/进度总览.md` 是状态面板与跨会话接力点：每个包的状态、关键产出、验证基线、待用户确认事项全部登记在案，任何新会话先读它。

## 强化机制二：交接简报

每个任务包会话结束时必须产出交付报告（`docs/交付报告/Sx-名称-交付报告.md`），固定含：
1. 产出文件清单；
2. 验收证据（每条验收标准：命令 + 输出摘要，不许虚报）；
3. 契约偏差 / 补登记录（无则写无）；
4. 遗留问题与标疑；
5. 给下游任务包的就绪度说明（下游拿什么、注意什么、有哪些在案缺口）；
6. **文档漂移检查（v1.2 新增）**：代码 vs 契约的 schema 校验结果必须贴证据，防「文档停在 v1、代码已到 v3」。

下一个会话靠它接力，不靠记忆。

## 强化机制三：需求追踪

- PRD 功能需求编号（F1/F2…）→ 归属任务包 → 验收证据，三者要有映射；
- 在任务拆解或进度总览里维护这张映射表；总装验收时按它查漏——没有任务包认领的需求是漏项，没有需求来源的功能是蔓延。

## 纪律清单（每个任务会话开工先自查）

1. 已读 PRD、接口契约、进度总览？没有就先读，别开工。
2. 只做自己任务包范围内的事，发现别的问题记录并报告，不顺手改。
3. 要改 PRD 或契约？停下，报告用户，确认后改文档升版本号再继续。
4. 交付必须带契约级测试并跑通；状态推进只认证据。
5. 会话结束时按「交接简报」格式写交付报告，更新进度总览状态。
6. **（v1.2 新增）不得以「模型上下文长」为由跳过拆分**——触发线是成本机制，长上下文重读成本随轮次累积，拆分照做。

## 参考实例

本法已在一个真实中型项目（市政管网算量与清单桌面软件，十余个任务包 S0~S11）完整走通：PRD、接口契约（含数据 schema 与 API 签名）、会话任务拆解（含开场白模板）、任务包计划书、交付报告、进度总览齐全；并验证过「中途插入新需求」的处理路径——PRD/契约/任务拆解先升版、补任务包再施工。新项目实施时可参照该文档结构与粒度。

## 语言策略

技能本体与文档结构保持通用；项目产物（PRD、计划书、报告等）用用户的语言书写；文件名、状态值、id 前缀等技术标识保持稳定不翻译。人用名称「项目拆分组装法」，技术标识 `multi-session-project` 不变。

## 常见坑

- PRD 写成流水账没有「不做的范围」——范围边界必须明确，否则会话各自发挥必跑偏。
- 契约只有口头约定没有 schema 示例和校验器——组装时才发现各说各话。
- 任务包切太大，单会话又爆了上下文——宁小勿大，半个会话能完成最好。
- 跳过集成会话直接宣布完工——契约级联调和 PRD 逐条验收不可省。
- 跳过跨文档一致性分析直接施工——缝隙会在组装时以十倍代价爆出来。
- 把「AI 说完成了」当完成——没有证据的状态推进等于没有推进。
- 以「模型上下文长」为由跳过拆分——省 token 是目的，拆分就是省 token 的手段。
- 文档与代码漂移不自检——交付报告必须附 schema 校验证据。

## 借鉴来源

- 前驱：GitHub spec-kit（跨文档一致性分析、收敛评估、澄清前置）；pi-sdd-kit（状态门、交接简报、可追踪 ID、EARS 句式、语言策略）。
- 2026 波（v1.2 补录）：AWS Kiro（「The Plan Is the Product」，规格即产品）；kspec / OpenSpec 的规格驱动工程思路。
- 用户原创：拆分组装法本质总结（总框架 → 拆分 → 细分框架 + 契约接口 → 组装）、省 token 触发线（20 万 tokens，成本机制）、命名「项目拆分组装法」。

## 版本记录

- v1.2（2026-08-15 定稿）：更名「项目拆分组装法」；「节省 token」升为一级核心目的；执行载体扩为四模式（新增 DSH 目标自动续轮、Ralph 验证轮）；新增「文档即跨工具事实源」原则；交付报告新增文档漂移检查；触发判据新增子代理 ≥4 维度；步骤编号整理为七步；新增「全流程总览」八步（补一致性分析/最小验证/分阶段校验/总验收/复盘 + 三条暗线，拆分增粒度平衡与易变隔离）。
- v1.1 吸收 GitHub spec-kit / pi-sdd-kit 长处（状态门、跨文档一致性分析、交接简报、需求追踪）。
